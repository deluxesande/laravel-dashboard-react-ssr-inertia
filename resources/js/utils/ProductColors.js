const ringColorMap = {
    white: "ring-white",
    gray: "ring-gray-500",
    black: "ring-black",
    // Add more colors as needed
};

const colorMap = {
    white: "bg-white",
    gray: "bg-gray-500",
    black: "bg-black",
    // Add more colors as needed
};

export default ringColorMap;
export { ringColorMap, colorMap };
